---
description: "Create a conventional commit that respects the repository's declared git mode and host profile: verifies before staging (the host's own verify command on a team host, the close-the-loop checklist and doctor's prove-before-done ladder on a solo host), stages explicit paths, and commits on the branch the git mode expects. WHEN: user invokes /commit, says \"commit this\", \"save my work\", \"make a commit\", or \"stage and commit\"; at the end of a session with a dirty tree. WHEN NOT: proving a change without committing it yet (/doctor); pushing or opening a pull request (only on a separate, explicit ask, never as part of \"finish this up\" or \"ship it\"); merging your own pull request (the host's review path, never this skill); read-only sessions with nothing to commit."
---
<!-- generated by harness/bootstrap/build_opencode_adapter.py; do not edit -->

Use the native `skill` tool to load `commit`, read its complete
`SKILL.md`, and follow that canonical procedure for the user's arguments:

$ARGUMENTS
