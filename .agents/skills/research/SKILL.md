---
name: research
description: "Verified research with independent claim-versus-source checking as the spine: a memory-first... WHEN: /research [deep|ingest] <question or source>..."
---

# Codex adapter for `research`

Read `harness/skills/research/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$research` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$research`. Canonical status: `spec-only`.
