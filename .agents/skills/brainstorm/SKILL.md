---
name: brainstorm
description: "Explore consequential ideas, designs, strategies, and trade-offs before commitment: credible... WHEN: /brainstorm, \"brainstorm\", \"explore options\"..."
---

# Codex adapter for `brainstorm`

Read `harness/skills/brainstorm/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$brainstorm` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$brainstorm`. Canonical status: `spec-only`.
