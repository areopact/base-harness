interface:
  display_name: "Skillify"
  short_description: "Turn a recurring failure pattern into a durable repair, starting with its existing owner. WHEN: user observes \"this keeps happening\", says..."
policy:
  allow_implicit_invocation: false
