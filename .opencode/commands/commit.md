---
description: "Create a conventional commit that respects the repository's declared git mode, stages explicit paths, and runs the close-the-loop checklist. WHEN: user invokes /commit, says \"commit this\", \"save my work\", \"make a commit\", or \"stage and commit\"; at the end of a session with a dirty tree. WHEN NOT: proving the change works first (/verify); pushing, merging, or rebasing (the host's git workflow, on request only); read-only sessions with nothing to commit."
---
<!-- generated by harness/bootstrap/build_opencode_adapter.py; do not edit -->

Use the native `skill` tool to load `commit`, read its complete
`SKILL.md`, and follow that canonical procedure for the user's arguments:

$ARGUMENTS
