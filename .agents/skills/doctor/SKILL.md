---
name: doctor
description: "Harness diagnostic from inside a session: run the three offline doctors and report per runtime... WHEN: user invokes /doctor, says \"test the harness\"..."
---

# Codex adapter for `doctor`

Read `harness/skills/doctor/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$doctor` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$doctor`. Canonical status: `spec-only`.
