---
name: verify
description: "Prove a change actually works by exercising it end to end before claiming done, and report the... WHEN: user invokes /verify, says \"prove this works\"..."
---

# Codex adapter for `verify`

Read `harness/skills/verify/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$verify` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$verify`. Canonical status: `spec-only`.
