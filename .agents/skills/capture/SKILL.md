---
name: capture
description: "Park a half-thought, observation, or pointer in one frictionless queue for later routing... WHEN: /capture <text>, \"drop this in the inbox\"..."
---

# Codex adapter for `capture`

Read `harness/skills/capture/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$capture` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$capture`. Canonical status: `spec-only`.
