---
name: skillify
description: "Turn a recurring failure pattern into a durable repair, starting with its existing owner. WHEN: user observes \"this keeps happening\", says..."
---

# Codex adapter for `skillify`

Read `harness/skills/skillify/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$skillify` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$skillify`. Canonical status: `spec-only`.
