interface:
  display_name: "Commit"
  short_description: "Create a conventional commit that respects the repository's declared git mode and host profile... WHEN: user invokes /commit, says \"commit this\"..."
policy:
  allow_implicit_invocation: false
