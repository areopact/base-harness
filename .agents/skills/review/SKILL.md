---
name: review
description: "Review specs, plans, decks, memos, decision docs, supplied plan text, and inspectable... WHEN: /review, \"review this spec\", \"red-team this\"..."
---

# Codex adapter for `review`

Read `harness/skills/review/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$review` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$review`. Canonical status: `spec-only`.
