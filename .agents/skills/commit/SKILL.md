---
name: commit
description: "Create a conventional commit that respects the repository's declared git mode and host profile... WHEN: user invokes /commit, says \"commit this\"..."
---

# Codex adapter for `commit`

Read `harness/skills/commit/SKILL.md` completely, then follow that canonical
procedure. Translate any `/skill` examples or `$ARGUMENTS` placeholders to the
Codex `$commit` invocation and the user's supplied arguments. This wrapper only
keeps Codex discovery metadata compact.

Invoke explicitly as `$commit`. Canonical status: `spec-only`.
